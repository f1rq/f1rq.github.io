version: '3'
services:
  app:
    image: wpkg/website
    container_name: website
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80" 
    environment:
      - PORT=80

